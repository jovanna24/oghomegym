/** @type {import('tailwindcss').Config} */
const config = { // Assign to a variable 'config'
   content: ["./src/**/*.{html,js,jsx}"], // Ensure .jsx is included here
   theme: {
     extend: {},
   },
   plugins: [],
 };

export default config; // Export the named variable